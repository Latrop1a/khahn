import moduleName from 'module';

const doms = {
  mobileMenuButton: document.querySelector('#mobile'),
};

const mobileMenuClickHandler = event => {};

//event listeners
addEventListener(doms.mobileMenuButton, 'click', mobileMenuClickHandler);
